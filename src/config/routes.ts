interface Routes {
  home: string;
  menu: string;
  shop: string;
  locations: string;
  contact: string;
}

const ROUTES: Routes = {
  home: "/",
  menu: "/menu",
  shop: "/shop",
  locations: "/locations",
  contact: "/contact",
};

export default ROUTES;
